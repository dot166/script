name: build emoji script

on:
  push:
    branches: [ "15-qpr2" ]
  pull_request:
    branches: [ "15-qpr2" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --verbose
